<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamx.exsite.model.mapper.user.UserMapper">
  
  <insert id="registerUser">
    INSERT INTO USER_INFO (USER_NO, NAME, USER_ID, USER_PW, EMAIL, PHONE, ADDRESS, METHOD)
    	VALUES (
    		SEQ_USER_NO.NEXTVAL,
    		#{name},
    		#{userId},
    		#{userPw},
    		#{email},
    		#{phone},
    		#{address},
    		'NORMAL'
    	)
  </insert>
  
  <insert id="registerWithNaver">
  	INSERT INTO USER_INFO (USER_NO, NAME, USER_ID, EMAIL, PHONE, METHOD)
  		VALUES (
  			SEQ_USER_NO.NEXTVAL,
  			#{name},
  			#{userId},
  			#{email},
  			#{phone},
  			#{method}
  		)
  </insert>
  
  <insert id="registerWithGoogle">
  	INSERT INTO USER_INFO (USER_NO, NAME, USER_ID, EMAIL, METHOD)
  		VALUES (
  			SEQ_USER_NO.NEXTVAL,
  			#{name},
  			#{userId},
  			#{email},
  			#{method}
  		)
  </insert>
  
  <select id="idCheck">
  	SELECT COUNT(*) 
  	FROM USER_INFO 
  	WHERE USER_ID = #{id} AND USER_STATUS = 'N'
  </select>
  
  
  <resultMap id="userInfo" type="UserDTO">
  	<result column="USER_ID" property="userId"/>
  	<result column="USER_PW" property="userPw"/>
  </resultMap>
  
  <select id="basicLogin" resultMap="userInfo">
  	SELECT USER_NO, NAME, USER_ID, USER_PW, EMAIL, PHONE, ADDRESS, CREATE_DATE, UPDATE_DATE, USER_STATUS, METHOD, GRADE
  	FROM USER_INFO
  	WHERE USER_ID = #{userId} AND METHOD = 'NORMAL' AND USER_STATUS = 'N'
  </select>
  
  <select id="mailCheck">
  	SELECT COUNT(*)
  	FROM USER_INFO
  	WHERE EMAIL = #{email}
  </select>
  
  <select id="phoneCheck">
  	SELECT COUNT(*)
  	FROM USER_INFO
  	WHERE PHONE = #{phone}
  </select>
  
  <select id="idSearch">
  	SELECT USER_ID
  	FROM USER_INFO
  	WHERE (EMAIL = #{authMethod} OR PHONE = #{authMethod}) AND METHOD = 'NORMAL'
  </select>
  
  <select id="searchUserName">
  	SELECT COUNT(*)
  	FROM USER_INFO
  	WHERE NAME = #{name}
  </select>
  
  <update id="passwordChange">
  	UPDATE USER_INFO
  	SET USER_PW = #{encodedPassword}
  	WHERE USER_ID = #{userId} AND NAME = #{name} AND USER_STATUS = 'N' AND (EMAIL = #{authMethod} OR PHONE = #{authMethod})
  </update>
  
  <select id="accountCheck">
  	SELECT COUNT(*)
  	FROM USER_INFO
  	WHERE METHOD = #{signupMethod} AND EMAIL = #{email}
  </select>
  
  <!-- 관리자 페이지 전체 유저 정보 불러오기 -->
  <select id="getAllUsers" resultType="com.teamx.exsite.model.user.dto.UserDTO">
        SELECT 
            USER_NO,
            NAME,
            USER_ID,
            USER_PW,
            EMAIL,
            PHONE,
            ADDRESS,
            CREATE_DATE,
            UPDATE_DATE,
            USER_STATUS,
            METHOD,
            GRADE
        FROM USER_INFO
    </select>
    
    <!-- 관리자 페이지 회원 검색하기 -->
    <select id="searchUsers" resultType="com.teamx.exsite.model.user.dto.UserDTO">
        SELECT 
            USER_NO,
            NAME,
            USER_ID,
            USER_PW,
            EMAIL,
            PHONE,
            ADDRESS,
            CREATE_DATE,
            UPDATE_DATE,
            USER_STATUS,
            METHOD,
            GRADE
        FROM USER_INFO
        WHERE NAME LIKE '%' || #{name} || '%'
    </select>
    
    <!-- 관리자 페이지 해당회원 정보 불러오기 -->
    <select id="findByUserNo" resultType="com.teamx.exsite.model.user.dto.UserDTO">
        SELECT * FROM USER_INFO WHERE USER_NO = #{userNo}
    </select>
    
    <!-- 관리자 페이지 해당회원 정보 수정하기 -->
    <update id="updateUserInfo">
        UPDATE USER_INFO
        SET USER_PW = #{userPw},
        	NAME = #{name},
            EMAIL = #{email},
            PHONE = #{phone},
            ADDRESS = #{address},
            GRADE = #{grade}
        WHERE USER_NO = #{userNo}
    </update>
    
    <!-- 관리자 페이지 해당회원 탈퇴 처리하기 -->
    <update id="updateUserStatus" parameterType="String">
	    UPDATE user_info
	    SET USER_STATUS = 'Y', UPDATE_DATE = SYSDATE
	    WHERE USER_ID = #{userId}
	</update>
	
</mapper>